<!--
目的：说明 AI PM JD skill 内置参考资料的边界。
定义：随 skill 一同分发的元模型、规则和报告契约目录。
范围包括：`meta-model.md`、`modeling-rules.md`、JSON schema 和完整报告契约。
范围不包括：运行脚本、测试 JD、外部文档链接或用户数据。
使用与修改规则：保持每份参考资料可脱离仓库独立阅读；修改后同步运行 skill 静态测试与人工会话验收。
-->

# references 目录说明

## 目的

为 AI PM JD skill 提供自包含的领域知识和输出约束。

## 定义

这是 skill 的按需加载资料，不承担执行逻辑。

## 范围包括

- 元模型、建模规则、完整 JSON schema 和报告契约。

## 范围不包括

- 本仓库应用代码、测试样本、密钥和网络资源。

## 使用与修改规则

- `SKILL.md` 必须明确引用所有用于默认分析的资料。
