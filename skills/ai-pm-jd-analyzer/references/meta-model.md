<!--
目的：定义 JD 自主分析可使用的完整业务元模型。
定义：岗位、工作、雇佣条件和风险之间的稳定概念、字段与关系词典。
范围包括：十类模型、受控关系与证据状态。
范围不包括：具体 JD 结论、简历信息或企业真实成熟度判断。
使用与修改规则：只使用有 JD 证据支持的节点；未披露内容标为 not_disclosed，不补写为事实。
-->

# AI PM JD 完整业务元模型

| 模型 | 说明 |
| --- | --- |
| 公司上下文 | 公司名称、行业、业务、客户、商业模式、阶段、组织、技术战略和招聘主体。 |
| 岗位画像 | 职位、职级、部门、汇报对象、地点、办公方式、用工类型、人数和定位。 |
| 业务工作 | 价值流及其按顺序的工作事项。 |
| 角色责任 | 岗位承担的流程角色，以及角色对事项的 RACI 责任。 |
| 业务实体 | 被事项 CRUD 的对象、抽象层级、生命周期与实体关系。 |
| 业务能力 | 稳定完成事项、管理实体的能力；采用 11 类能力分类。 |
| 任职要求 | 候选人门槛、必要程度、客观性与业务映射。 |
| 工作环境 | 协作、项目并发、节奏、强度、出差、驻场和决策约束。 |
| 薪酬福利 | 薪资、奖金、股权、社保、补贴、假期、培训等雇佣待遇。 |
| 风险与不确定性 | 映射异常、职责过载、生命周期缺口、信息缺口及其待确认项。 |

## 证据状态

- `explicit`：JD 原文直接表达；必须包含短证据摘录。
- `inferred`：由已记录的元素或关系谨慎推出；必须记录依据。
- `not_disclosed`：JD 未披露；不附伪证据，也不自动视为风险。

## 核心关系

- 岗位承担角色；角色以 `responsible`、`accountable`、`consulted` 或 `informed` 参与工作事项。
- 价值流包含有序工作事项；工作事项对业务实体执行 CRUD，并依赖业务能力。
- 每个业务实体只能归属一个 `primary_capability_id`。
- 实体关系只可使用 `parent_of`、`lifecycle_precedes`、`input_to`、`related_to`；能力关系只可使用 `depends_on`。
- 任职要求可直接证明事项、支撑能力、适用于角色或环境、约束岗位；工作环境影响事项和要求。
- 风险由元素或关系异常推导，必须区分事实、推断与待确认项。

## 业务实体与能力

业务实体只能归入一个域：`business`、`product`、`data_knowledge`、`ai_capability`、`software_system`、`runtime_delivery` 或 `governance_constraint`。实体抽象层级为 `concept`、`type` 或 `specialized_type`。

业务能力使用以下 11 类作为分类，而非把工具名、人格词或一次性动作当能力：业务场景管理、需求管理、产品方案管理、AI 能力设计与管理、数据与知识管理、AI 评测与质量管理、产品研发与交付管理、产品运营与迭代管理、项目与组织协同管理、行业业务与合规管理、AI 技术洞察与能力规划。
